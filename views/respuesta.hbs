<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manolo y España</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #f5f5f5;
        }

        .container {
            text-align: center;
            background-color: #fff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            font-size: 2em;
            color: #333;
        }

        .flag {
            margin-top: 20px;
            width: 150px;
        }

        .info {
            margin-top: 20px;
            font-size: 1.2em;
            color: #555;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>{{nombre}} proviene de {{paisDeOrigen}}</h1>
        <img class="flag" src="{{bandera}}" alt="Bandera de {{paisDeOrigen}}">
        <p class="info">¡Saludos desde {{paisDeOrigen}}!</p>
        {{#if esArgentina}}
            <p>CAMPEONES DEL MUNDO</p>
        {{/if}}
    </div>
</body>
</html>
